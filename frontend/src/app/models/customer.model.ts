import { Address } from "./address.model";


export class Customer {
    personId?: string;
    firstName?: string;
    lastName?: string;
    address?: Address;
    phonenumber?: string;
    email?: string;
}